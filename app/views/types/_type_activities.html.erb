<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Activities

    <a data-toggle="collapse" href="#type_activities_new_form">
      Add activity
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @type.activities.each do |activity| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to activity, activity %>

        <% if current_user == activity.owner %>
          <%= link_to activity, method: :delete do %>
            <i class="fas fa-trash-alt"></i>
          <% end %>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="type_activities_new_form">
        <%= render partial: "types/type_activities_new_form" %>
      </div>
    </div>

  </ul>
</div>
